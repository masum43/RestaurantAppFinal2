<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.restaurantappdemo2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.AccountActiity"></activity>
        <activity android:name=".Activity.ReportActivity" />
        <activity android:name=".Activity.SingleItemReviewActivity" />
        <activity android:name=".Activity.ItemReviewActivity" />
        <activity android:name=".Activity.EditOrderActivity" />
        <activity android:name=".Activity.ServiceActivity" />
        <activity android:name=".Activity.SearchItemActivity" />
        <activity android:name=".Activity.AddItemActivity" />
        <activity
            android:name=".Activity.MainActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>